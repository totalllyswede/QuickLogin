<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        mc:Ignorable="d" 
        x:Class="QuickLogin.MainWindow"
        Title="QuickLogin"
        Height="480" 
        Width="550"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1A1A2E" 
        WindowStyle="None" 
        BorderBrush="#3D3D5C" 
        BorderThickness="1"
        AllowsTransparency="True">

    <Window.Resources>
        <!-- Primary Action Button -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#0D7377"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#14FFEC"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#0A5F62"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Button -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2E2E48"/>
            <Setter Property="Foreground" Value="#E4E4E4"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3D3D5C"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3D3D5C"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#252538"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Help Button Style -->
        <Style x:Key="HelpButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#14FFEC"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2E2E48"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Main Content -->
        <Grid Margin="30,20,30,30">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <!-- Header -->
                <RowDefinition Height="80"/>
                <!-- Account Selection -->
                <RowDefinition Height="70"/>
                <!-- WoW Path -->
                <RowDefinition Height="50"/>
                <!-- Launch Checkbox -->
                <RowDefinition Height="*"/>
                <!-- Spacer -->
                <RowDefinition Height="50"/>
                <!-- Account Management -->
                <RowDefinition Height="70"/>
                <!-- Login Buttons -->
            </Grid.RowDefinitions>

            <!-- Header with Title and Help -->
            <Grid Grid.Row="0">
                <TextBlock Text="QuickLogin" 
                           FontFamily="Segoe UI Light" 
                           FontSize="32" 
                           Foreground="White" 
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"/>
                <Button Content="?" 
                        Style="{StaticResource HelpButton}"
                        Click="Help_Click"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        ToolTip="Help"/>
            </Grid>

            <!-- Account Selection -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock Text="Select Account" 
                           FontFamily="Segoe UI" 
                           FontSize="13" 
                           Foreground="#B0B0B0" 
                           Margin="0,0,0,8"/>
                <ComboBox x:Name="AccountCombo" 
                          DisplayMemberPath="DisplayName"
                          FontFamily="Segoe UI"
                          FontSize="16"
                          Height="42"
                          Background="#2E2E48"
                          Foreground="White"
                          BorderBrush="#3D3D5C"
                          BorderThickness="1"
                          SelectionChanged="AccountCombo_SelectionChanged">
                    <ComboBox.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#2E2E48"/>
                        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#2E2E48"/>
                        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#2E2E48"/>
                        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="White"/>
                    </ComboBox.Resources>
                    <ComboBox.Template>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton"
                                            Grid.Column="2"
                                            Focusable="False"
                                            IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                            ClickMode="Press"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="5">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition/>
                                                        <ColumnDefinition Width="30"/>
                                                    </Grid.ColumnDefinitions>
                                                    <ContentPresenter Grid.Column="0"
                                                                    Margin="12,0,0,0"
                                                                    VerticalAlignment="Center"
                                                                    HorizontalAlignment="Left"/>
                                                    <Path Grid.Column="1"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Data="M 0 0 L 4 4 L 8 0 Z"
                                                          Fill="White"/>
                                                </Grid>
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter x:Name="ContentSite"
                                                IsHitTestVisible="False"
                                                Content="{TemplateBinding SelectionBoxItem}"
                                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                Margin="12,0,30,0"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Left">
                                    <ContentPresenter.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="White"/>
                                        </Style>
                                    </ContentPresenter.Resources>
                                </ContentPresenter>
                                <Popup x:Name="Popup"
                                       Placement="Bottom"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       PopupAnimation="Slide">
                                    <Border Background="#2E2E48"
                                            BorderBrush="#3D3D5C"
                                            BorderThickness="1"
                                            CornerRadius="5">
                                        <Grid x:Name="DropDown"
                                              SnapsToDevicePixels="True"
                                              MinWidth="{TemplateBinding ActualWidth}"
                                              MaxHeight="200">
                                            <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                                <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                            </ScrollViewer>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </ComboBox.Template>
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="FontFamily" Value="Segoe UI"/>
                            <Setter Property="FontSize" Value="16"/>
                            <Setter Property="Padding" Value="12,10"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ComboBoxItem">
                                        <Border x:Name="Border"
                                                Background="{TemplateBinding Background}"
                                                BorderThickness="0"
                                                Padding="{TemplateBinding Padding}"
                                                Margin="2,1">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsHighlighted" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#0D7377"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#0A5F62"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>
            </StackPanel>

            <!-- WoW Path Selection -->
            <StackPanel Grid.Row="2" VerticalAlignment="Center">
                <TextBlock Text="World of Warcraft Executable (Optional)" 
                           FontFamily="Segoe UI" 
                           FontSize="13" 
                           Foreground="#B0B0B0" 
                           Margin="0,0,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0"
                            Background="#2E2E48"
                            BorderBrush="#3D3D5C"
                            BorderThickness="1"
                            CornerRadius="5,0,0,5"
                            Padding="12,0"
                            Height="42">
                        <TextBlock x:Name="ExePathText" 
                                   Text="Not set" 
                                   FontFamily="Segoe UI"
                                   FontSize="14"
                                   Foreground="#808080"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"/>
                    </Border>
                    <Button Grid.Column="1"
                            Content="Browse" 
                            Style="{StaticResource SecondaryButton}"
                            Click="BrowseWowExe_Click"
                            Height="42"
                            Padding="20,0"/>
                </Grid>
            </StackPanel>

            <!-- Launch Checkbox -->
            <CheckBox x:Name="LaunchWowCheckbox"
                      Grid.Row="3"
                      Content="Auto-launch game when logging in"
                      FontFamily="Segoe UI"
                      FontSize="14"
                      Foreground="#E4E4E4"
                      VerticalAlignment="Center"
                      Margin="2,0,0,0"
                      Checked="LaunchWowCheckbox_Changed"
                      Unchecked="LaunchWowCheckbox_Changed"/>

            <!-- Account Management Buttons -->
            <UniformGrid Grid.Row="5" 
                         Columns="2" 
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Center">
                <Button Content="+ Add Account" 
                        Style="{StaticResource SecondaryButton}" 
                        Click="AddAccount_Click"
                        Margin="0,0,5,0"/>
                <Button Content="− Remove Account" 
                        Style="{StaticResource SecondaryButton}" 
                        Click="RemoveAccount_Click"
                        Margin="5,0,0,0"/>
            </UniformGrid>

            <!-- Login Buttons -->
            <UniformGrid Grid.Row="6" 
                         Columns="3"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Bottom">
                <Button Content="Login &amp; Close"
                        Style="{StaticResource PrimaryButton}" 
                        Click="TypeCredentials_Click"
                        Margin="0,0,5,0"/>
                <Button Content="Login"
                        Style="{StaticResource PrimaryButton}" 
                        Click="TypeCredentials_ClickStay"
                        Margin="5,0,5,0"/>
                <Button Content="Exit"
                        Style="{StaticResource SecondaryButton}" 
                        Click="Close_Click"
                        Margin="5,0,0,0"/>
            </UniformGrid>
        </Grid>
    </Grid>
</Window>